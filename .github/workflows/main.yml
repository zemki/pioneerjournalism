on: push
name: Publish CSV
jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get Latest Code
      uses: actions/checkout@v3
    
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: ${{ secrets.server_address }}
        remote-user: zemki
        private-ssh-key: ${{ secrets.SSH_KEY }}
        destination-path: /var/www/html/pionierjournalismus/data/
